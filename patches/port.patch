diff --git a/.gitignore b/.gitignore
index 5be7eae..4478aa1 100644
--- a/.gitignore
+++ b/.gitignore
@@ -33,7 +33,7 @@ install
 # generated file
 src/imods.tar.gz
 src/js/crc_imods.json
-webapp/browser.js
+# webapp/browser.js is a committed static file in the offline build
 webAppModTrees.json
 
 /tools/mspfa/*/
@@ -51,4 +51,19 @@ build
 
 sessions
 
-db.sqlite
\ No newline at end of file
+db.sqlite
+# Android build outputs
+android/.gradle/
+android/app/build/
+android/app/src/main/assets/
+android/local.properties
+android/build/
+# generated intermediates
+src/imods.tar.gz
+build/webAppModTrees.json
+
+# but keep our build scripts
+!build/genModTrees.js
+
+# signing keys live inside the project folder but must never be committed
+/keys/
diff --git a/Makefile b/Makefile
index c1c27c2..2c2884c 100644
--- a/Makefile
+++ b/Makefile
@@ -47,23 +47,13 @@ src/imods.tar.gz: $(wildcard src/imods/*) $(wildcard src/imods/*/*)
 src/js/crc_imods.json: src/imods.tar.gz
 	yarn exec node src/js/validation.js src/imods/ src/js/crc_imods.json
 
-# browser.js must be built with known environment variables, not static
+# Offline build: webapp/browser.js is a static, committed file (no jinja2),
+# and the mod tree is generated from the repo's own src/imods (no ASSET_DIR,
+# no `tree`, no `jq`).
 WEBAPP_INTERMEDIATE=build/webAppModTrees.json
 
-build/webAppModTrees.json: webapp/browser.js.j2 .env
-	mkdir -p build/
-	# External mod support removed - only scan imods directory
-	# Source .env to get ASSET_DIR
-	bash -c 'set -a; source .env; set +a; cd "$${ASSET_DIR}"; tree archive/imods -J | jq '"'"'. | walk(if type == "object" then (if .type == "file" then ({"key": (.name), "value": true}) elif has("contents") then {"key": (.name), "value": .contents|from_entries} else . end) else . end) | .[:-1] | from_entries'"'"'' > build/webAppModTrees.json
-
-# Requires `python3 -m pip install jinja2-cli`
-.PHONY: webapp/browser.js
-webapp/browser.js:
-	env APP_VERSION=`jq -r '.version' < package.json` \
-		ASSET_PACK_HREF="${ASSET_PACK_HREF}" \
-		ASSET_DIR="${ASSET_DIR}" \
-		AUTH_SERVER_URL="${AUTH_SERVER_URL}" \
-			jinja2 webapp/browser.js.j2 > webapp/browser.js
+build/webAppModTrees.json: $(wildcard src/imods/*) $(wildcard src/imods/*/*)
+	node build/genModTrees.js
 
 # src/js/crc_pack.json:
 # 	yarn exec node src/js/validation.js "${ASSET_DIR}" src/js/crc_pack.json
@@ -103,14 +93,8 @@ serve: install ${SHARED_INTERMEDIATE} ${WEBAPP_INTERMEDIATE} webapp/browser.js
 
 .PHONY: build
 build: install ${SHARED_INTERMEDIATE} ${WEBAPP_INTERMEDIATE}
-	env ASSET_DIR="${ASSET_DIR}" \
-		ASSET_PACK_HREF="${ASSET_PACK_HREF}" \
-		AUTH_SERVER_URL="${AUTH_SERVER_URL}" \
-			make webapp/browser.js
 	env NODE_OPTIONS=--max_old_space_size=8192 \
-		ASSET_DIR="${ASSET_DIR}" \
-		ASSET_PACK_HREF="${ASSET_PACK_HREF}" \
-		AUTH_SERVER_URL="${AUTH_SERVER_URL}" \
+		ASSET_PACK_HREF="/assetpack/" \
 			yarn run vue-cli-service build webapp/browser.js
 			
 			
diff --git a/public/index.html b/public/index.html
index 63eb16c..c15df93 100644
--- a/public/index.html
+++ b/public/index.html
@@ -3,7 +3,9 @@
   <head>
     <meta charset="utf-8">
     <meta http-equiv="X-UA-Compatible" content="IE=edge">
-    <meta name="viewport" content="initial-scale=0.0, width=650">
+    <!-- Fixed 650px layout, scaled to fit the screen: preserves the original
+         Homestuck page proportions instead of reflowing them. -->
+    <meta name="viewport" content="width=650">
     <link rel="icon" href="<%= BASE_URL %>icons/default.ico">
     <title><%= htmlWebpackPlugin.options.title %></title>
     <meta name="og:title" content="The Unofficial Homestuck Collection Web Beta (Static)">
@@ -92,5 +94,4 @@
     <div id="app"></div>
     <!-- built files will be auto injected -->
   </body>
-  <script data-goatcounter="https://tuhc.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
 </html>
diff --git a/src/App.vue b/src/App.vue
index 43f1932..92c1087 100644
--- a/src/App.vue
+++ b/src/App.vue
@@ -28,7 +28,7 @@
         :ref="$localData.tabData.activeTabKey"
         :tabKey="$localData.tabData.activeTabKey"
       />
-      <MobileNotification />
+      <TouchControls ref="touchControls" />
       <ContextMenu :class="theme" ref="contextMenu" v-if="!$isWebApp" />
       <Updater ref="Updater" v-if="!$isWebApp" />
       <GuestBanner ref="GuestBanner" v-if="$root.guestMode" />
@@ -50,7 +50,6 @@
       <Setup ref="uistyle" />
       <ContextMenu ref="contextMenu" />
     </div>
-    <AuthModal :isVisible="showAuthModal" @auth-success="handleAuthSuccess" />
   </div>
 </template>
 
@@ -59,7 +58,6 @@ import Mods from "./mods.js";
 
 import Setup from "@/components/SystemPages/Setup.vue";
 import AppHeader from "@/components/AppMenu/AppHeader.vue";
-import AuthModal from "@/components/Auth/AuthModal.vue";
 
 const Notifications = () => import("@/components/UIElements/Notifications.vue");
 
@@ -95,20 +93,18 @@ export default {
     TabFrame,
     ContextMenu,
     Notifications,
-    MobileNotification: () =>
-      import("@/components/UIElements/MobileNotification.vue"),
+    TouchControls: () =>
+      import("@/components/UIElements/TouchControls.vue"),
     UrlTooltip,
     Updater,
-    GuestBanner,
-    AuthModal
+    GuestBanner
   },
   data() {
     return {
       zoomLevel: 0,
       needCheckTheme: false,
       stylesheets: [], // Mod optimization
-      showAuthModal: false, // New data property for auth modal visibility
-      sessionChecked: false // New data property to track session check completion
+      sessionChecked: false // Set true once persisted state has loaded
     };
   },
   computed: {
@@ -155,7 +151,10 @@ export default {
       // (it's because $refs isn't reactive)
       const tab_components = this.$refs[this.$localData.tabData.activeTabKey];
       if (tab_components) {
-        return tab_components[0];
+        // v-for refs are arrays (electron); the single webapp TabFrame isn't
+        return Array.isArray(tab_components)
+          ? tab_components[0]
+          : tab_components;
       }
       return undefined;
     },
@@ -280,22 +279,6 @@ export default {
         }
       });
     },
-    handleAuthSuccess(data) {
-      // Update the Vue instance's data properties directly to ensure reactivity
-      this.$localData.root.saveData = data.saveData;
-      this.$localData.root.settings = data.settings;
-      this.$localData.root.isAuthenticated = true;
-      this.showAuthModal = false;
-
-      // Reload local data from server to ensure session/localStorage are in sync
-      this.$localData.root.reloadLocalStorage().then(() => {
-        this.$logger.info("Local data reloaded after login/signup:", {
-          saveData: this.$localData.root.saveData,
-          settings: this.$localData.root.settings,
-          isAuthenticated: this.$localData.root.isAuthenticated
-        });
-      });
-    }
   },
   watch: {
     theme(to, from) {
@@ -305,16 +288,6 @@ export default {
       if (to != undefined) this.$root.tabTheme = to;
     }
   },
-  watch: {
-    sessionChecked(to, from) {
-      if (to === true) {
-        // Session check has completed, now decide whether to show auth modal
-        if (!this.$localData.root.isAuthenticated) {
-          this.showAuthModal = true;
-        }
-      }
-    }
-  },
   updated() {
     if (this.$isWebApp) this.filterLinksAndImages(window.document.body);
   },
diff --git a/src/components/SpecialPages/fullscreenFlash.vue b/src/components/SpecialPages/fullscreenFlash.vue
index 97af793..2814a9d 100644
--- a/src/components/SpecialPages/fullscreenFlash.vue
+++ b/src/components/SpecialPages/fullscreenFlash.vue
@@ -1,6 +1,6 @@
 <template>
   <div>
-    <GenericPage v-if="gameOverPreload" class="gameOverPreload" >
+    <GenericPage v-if="gameOverPreload && $root.dsRole !== 'text'" class="gameOverPreload" >
       <div class="pageContent">
         <div class="mediaContent">
           <h2 class="pageTitle">[S] GAME OVER.</h2>
@@ -21,7 +21,8 @@
         <div class="pageContent">
           <Footnotes :pageId="thisPage.pageId" preface />
           <div class="mediaContent">
-              <Media :url="flashUrl" ref="flash" />
+              <!-- dual-screen text role must not boot the Flash -->
+              <Media :url="flashUrl" ref="flash" v-if="$root.dsRole !== 'text'" />
           </div>
           <div class="textContent">
               <PageNav :thisPage="thisPage" ref="pageNav" :nextPages="nextPagesArray" :class="(needsNav ? '' : 'hidden')" />
diff --git a/src/components/SpecialPages/x2Combo.vue b/src/components/SpecialPages/x2Combo.vue
index 5f08169..464e77a 100644
--- a/src/components/SpecialPages/x2Combo.vue
+++ b/src/components/SpecialPages/x2Combo.vue
@@ -12,7 +12,7 @@
         <Footnotes :pageId="thisPages[0].pageId" preface />
           <div class="mediaContent">
             <h2 class="pageTitle" v-text="thisPages[0].title" />
-            <div class="media">
+            <div class="media" v-if="$root.dsRole !== 'text'">
               <Media v-for="url in pageMedia[0]" :key="url" :url="url" class="panel"/>
             </div>
           </div>      
@@ -26,7 +26,7 @@
         <Footnotes :pageId="thisPages[1].pageId" preface />
           <div class="mediaContent">
             <h2 class="pageTitle" v-html="thisPages[1].title" />
-            <div class="media">
+            <div class="media" v-if="$root.dsRole !== 'text'">
               <Media v-for="url in pageMedia[1]" :key="url" :url="url" class="panel"/>
             </div>
           </div>      
diff --git a/src/components/StoryPage/Page.vue b/src/components/StoryPage/Page.vue
index 3dfd217..4843b89 100644
--- a/src/components/StoryPage/Page.vue
+++ b/src/components/StoryPage/Page.vue
@@ -22,7 +22,10 @@
             v-if="!supercartridge"
             ref="pageTitle"
           />
-          <div class="media" ref="media">
+          <!-- On the dual-screen text role the media must not mount at all:
+               CSS hiding alone would still boot Ruffle/Flash on the bottom
+               screen. -->
+          <div class="media" ref="media" v-if="$root.dsRole !== 'text'">
             <Media
               v-for="url in pageMedia"
               :key="url"
@@ -66,7 +69,7 @@
         </div>
         <Footnotes :pageId="thisPage.pageId" class="footnotesContainer" />
       </div>
-      <div class="hidden">
+      <div class="hidden" v-if="$root.dsRole !== 'text'">
         <!-- Preload images -->
         <Media
           v-for="url in nextPagesMedia"
@@ -353,7 +356,8 @@ export default {
     window.removeEventListener("message", this.handleIframeMessage);
   },
   updated() {
-    if (this.hscroll) this.$refs.media.scrollLeft = 0;
+    // media ref is absent on the dual-screen text role
+    if (this.hscroll && this.$refs.media) this.$refs.media.scrollLeft = 0;
   }
 };
 </script>
diff --git a/src/components/SystemPages/Settings.vue b/src/components/SystemPages/Settings.vue
index 5c4e27e..0a9491f 100644
--- a/src/components/SystemPages/Settings.vue
+++ b/src/components/SystemPages/Settings.vue
@@ -341,6 +341,91 @@
       </div>
     </div>
 
+    <div class="card">
+      <div class="settings touchcontrols">
+        <h2>Touch Controls</h2>
+        <p class="settingDesc">
+          Transparent on-screen controls for phones and tablets: a virtual
+          joystick and action buttons that send keyboard input to interactive
+          Flash pages (the walkaround games), plus optional page-turn aids.
+        </p>
+        <dl>
+          <template v-for="boolSetting in touchListBoolean">
+            <dt :key="boolSetting.model + '-dt'">
+              <label>
+                <input
+                  type="checkbox"
+                  :name="boolSetting.model"
+                  v-model="$localData.settings[boolSetting.model]"
+                  @click="toggleSetting(boolSetting.model)"
+                />{{ boolSetting.label }}</label
+              >
+            </dt>
+            <label :key="boolSetting.model + '-dd'" :for="boolSetting.model">
+              <dd class="settingDesc" v-html="boolSetting.desc" />
+            </label>
+          </template>
+
+          <template v-if="$localData.settings.touchControls">
+            <dt>
+              <label class="fontslider">
+                Control opacity:
+                <span
+                  v-text="
+                    Math.round(($localData.settings.touchOpacity || 0.4) * 100) +
+                      '%'
+                  "
+                />
+                <input
+                  type="range"
+                  v-model.number="$localData.settings.touchOpacity"
+                  min="0.15"
+                  max="0.9"
+                  step="0.05"
+                  @change="$localData.root.saveLocalStorage()"
+                />
+              </label>
+            </dt>
+            <dd class="settingDesc">
+              How visible the joystick and buttons are over the page.
+            </dd>
+            <dt>
+              <label class="fontslider">
+                Control size:
+                <span
+                  v-text="
+                    Math.round(($localData.settings.touchScale || 1) * 100) + '%'
+                  "
+                />
+                <input
+                  type="range"
+                  v-model.number="$localData.settings.touchScale"
+                  min="0.6"
+                  max="1.6"
+                  step="0.1"
+                  @change="$localData.root.saveLocalStorage()"
+                />
+              </label>
+            </dt>
+            <dd class="settingDesc">
+              Scales the joystick and buttons up or down.
+            </dd>
+            <dt>
+              <button @click="$localData.temp.touchEdit = true">
+                Adjust control positions
+              </button>
+              <button @click="resetTouchLayout()">
+                Reset positions
+              </button>
+            </dt>
+            <dd class="settingDesc">
+              Drag the joystick and button cluster wherever you like.
+            </dd>
+          </template>
+        </dl>
+      </div>
+    </div>
+
     <div class="card">
       <div class="settings controversial">
         <!-- TODO v-if="$isNewReader"> -->
@@ -535,6 +620,37 @@ export default {
     return {
       log,
       settingListBoolean: [],
+      touchListBoolean: [
+        {
+          model: "touchControls",
+          label: "Enable touch controls",
+          desc:
+            "Master switch for the on-screen overlay. When off, nothing is drawn over the page."
+        },
+        {
+          model: "touchAutoShow",
+          label: "Auto-show on Flash pages",
+          desc:
+            "The joystick and buttons appear automatically on pages with interactive Flash content, and hide during normal reading. The small handle on the right edge toggles them manually at any time."
+        },
+        {
+          model: "touchNavArrows",
+          label: "Page-turn arrows",
+          desc:
+            "Transparent previous/next arrows on the screen edges for one-handed reading."
+        },
+        {
+          model: "touchSwipeNav",
+          label: "Swipe navigation",
+          desc:
+            "Flick left or right anywhere on a page to go to the next or previous page. Ignored over Flash content."
+        },
+        {
+          model: "touchHaptics",
+          label: "Haptic feedback",
+          desc: "A tiny vibration when a control is pressed."
+        }
+      ],
       enhancementListBoolean: [
         {
           model: "arrowNav",
@@ -558,8 +674,7 @@ export default {
           model: "bandcampEmbed",
           label: "Online bandcamp player",
           desc:
-            "Although the vast majority of this collection works offline, the music database allows you to use Bandcamp's online player to legally play tracks from the source. You can disable this if you don't want the collection connecting to the internet.",
-          platform_whitelist: ["electron"]
+            "Although the vast majority of this collection works offline, the music database allows you to use Bandcamp's online player to legally play tracks from the source. This is the only feature that connects to the internet; it is off by default in this offline build."
         }
       ],
       settingListSystem: [],
@@ -877,6 +992,10 @@ export default {
         }, 1000);
       }
     },
+    resetTouchLayout() {
+      this.$set(this.$localData.settings, "touchLayout", null);
+      this.$localData.root.saveLocalStorage();
+    },
     factoryReset() {
       const args = {
         title: "FACTORY RESET",
diff --git a/src/components/UIElements/MediaEmbed.vue b/src/components/UIElements/MediaEmbed.vue
index 7654b8c..77f1e62 100644
--- a/src/components/UIElements/MediaEmbed.vue
+++ b/src/components/UIElements/MediaEmbed.vue
@@ -215,19 +215,22 @@ export default {
     ruffleEmbed() {
       // At some point between 2025.3.14 and 2025.4.13 ruffle stopped supporting our old runtime. Damn. -->
       if (this.$localData.settings.ruffleFallback) {
-        let scriptTag;
-        if (this.$isWebApp) {
-          scriptTag =
-            '<script src="https://unpkg.com/@ruffle-rs/ruffle"><\/script>';
-        } else {
-          scriptTag = `<script src="${this.$getResourceURL(
-            "assets://js/ruffle/ruffle.js"
-          )}"><\/script>`;
-        }
+        // load ruffle from the asset pack, same path as the electron build
+        const scriptTag = `<script src="${this.$getResourceURL(
+          "assets://js/ruffle/ruffle.js"
+        )}"><\/script>`;
         // Inject Ruffle config for iOS Safari workaround (see ruffle#10620)
         return `
           <script>
             (function() {
+              window.RufflePlayer = window.RufflePlayer || {};
+              window.RufflePlayer.config = window.RufflePlayer.config || {};
+              // the webview host allows autoplay, so skip ruffle's
+              // click-to-play/unmute gates and use our own loading screen
+              window.RufflePlayer.config.autoplay = "on";
+              window.RufflePlayer.config.unmuteOverlay = "hidden";
+              window.RufflePlayer.config.splashScreen = false;
+              window.RufflePlayer.config.preloader = false;
               function isIOS() {
                 return /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
               }
@@ -235,8 +238,6 @@ export default {
                 return /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
               }
               if (isIOS() && isSafari()) {
-                window.RufflePlayer = window.RufflePlayer || {};
-                window.RufflePlayer.config = window.RufflePlayer.config || {};
                 window.RufflePlayer.config.preferredRenderer = "webgl";
               }
             })();
@@ -254,7 +255,58 @@ export default {
         <style>
           body{margin:0;overflow:hidden;background:${this.flashProps.bgcolor}}
           object{${this.flashProps.rawStyle}}
+          #tuhcFlashDiag{position:fixed;left:0;right:0;bottom:0;z-index:99999;
+            background:rgba(120,0,0,0.92);color:#fff;font:11px/1.5 monospace;
+            padding:6px 8px;white-space:pre-wrap;word-break:break-all;display:none}
+          #tuhcLoad{position:fixed;inset:0;z-index:9999;
+            background:${this.flashProps.bgcolor};
+            display:flex;align-items:center;justify-content:center;
+            transition:opacity .35s;}
+          #tuhcLoad.done{opacity:0;pointer-events:none}
+          #tuhcLoad svg{width:96px;height:96px;
+            animation:tuhcPulse 1.1s ease-in-out infinite}
+          @keyframes tuhcPulse{
+            0%,100%{opacity:.35;transform:scale(.96)}
+            50%{opacity:1;transform:scale(1)}}
         </style>
+        <script>
+          // surface loader failures instead of a silent blank box
+          (function () {
+            var msgs = [];
+            window.__tuhcFlashDiag = function (m) {
+              msgs.push(m);
+              var el = document.getElementById("tuhcFlashDiag");
+              if (!el) {
+                el = document.createElement("div");
+                el.id = "tuhcFlashDiag";
+                (document.body || document.documentElement).appendChild(el);
+              }
+              el.style.display = "block";
+              el.textContent =
+                "FLASH PLAYER PROBLEM\\n" + msgs.join("\\n") +
+                "\\n" + navigator.userAgent;
+            };
+            window.addEventListener("error", function (e) {
+              if (e && e.target && (e.target.src || e.target.href)) {
+                window.__tuhcFlashDiag(
+                  "Failed to load: " + (e.target.src || e.target.href)
+                );
+              } else if (e && e.message) {
+                window.__tuhcFlashDiag(
+                  "Script error: " + e.message + " @" +
+                  String(e.filename || "").split("/").pop() + ":" + e.lineno
+                );
+              }
+            }, true);
+            setTimeout(function () {
+              if (!window.RufflePlayer || !window.RufflePlayer.newest) {
+                window.__tuhcFlashDiag("Ruffle never initialized (script missing or too old a WebView).");
+              } else if (!document.querySelector("ruffle-player,ruffle-embed,ruffle-object")) {
+                window.__tuhcFlashDiag("Ruffle loaded but no player was created.");
+              }
+            }, 8000);
+          })();
+        <\/script>
         <script>
           // JS Enhancements: ${this.$localData.settings.jsFlashes}
           // HQ Audio: ${this.$localData.settings.hqAudio}
@@ -360,6 +412,32 @@ export default {
         ${this.ruffleEmbed}
         </head>
         <body>
+        <div id="tuhcLoad">
+          <svg viewBox="0 0 32 32" aria-label="Loading">
+            <g fill="#3da80d">
+              <rect x="23" y="3" width="4" height="7"/>
+              <path d="M16 2 L30 15 H26 V29 H19 V20 H13 V29 H6 V15 H2 Z"/>
+            </g>
+          </svg>
+        </div>
+        <script>
+          // drop the loading screen once ruffle has the movie header
+          (function () {
+            var t0 = Date.now();
+            var iv = setInterval(function () {
+              var rp = document.querySelector("ruffle-player,ruffle-embed,ruffle-object");
+              var ready = rp && (rp.metadata || (rp.readyState !== undefined && rp.readyState >= 1));
+              if (ready || Date.now() - t0 > 20000) {
+                clearInterval(iv);
+                var el = document.getElementById("tuhcLoad");
+                if (el) {
+                  el.className = "done";
+                  setTimeout(function () { el.remove(); }, 500);
+                }
+              }
+            }, 200);
+          })();
+        <\/script>
         <object type="application/x-shockwave-flash"
           width="${this.flashProps.width}"
           height="${this.flashProps.height}"
diff --git a/src/main.js b/src/main.js
index 2fdb9dc..18edff6 100644
--- a/src/main.js
+++ b/src/main.js
@@ -210,9 +210,12 @@ Vue.mixin({
       return resolvedUrl;
     },
     $openModal(to) {
-      this.$root.$children[0].$refs[
+      // v-for refs are arrays (electron); the single webapp TabFrame isn't
+      const ref = this.$root.$children[0].$refs[
         this.$localData.tabData.activeTabKey
-      ][0].openModal(to);
+      ];
+      const frame = Array.isArray(ref) ? ref[0] : ref;
+      if (frame) frame.openModal(to);
     },
     $openLink(url, auxClick = false) {
       // Open a link. Could be intra-app, external, or an assets:// uri
@@ -652,7 +655,11 @@ Promise.all(promises_loading).then(_ => {
         loadStage: undefined,
         guestMode: false,
         platform: window.isWebApp ? "webapp" : "electron",
-        tabTheme: {} // Modified by App (avoid reacting to refs)
+        tabTheme: {}, // Modified by App (avoid reacting to refs)
+        // Dual-screen role, set by the Android shell's injected JS:
+        // null (single screen), "panel" (top screen) or "text" (bottom
+        // screen). Components use it to skip mounting heavy media.
+        dsRole: window.__TUHC_DS_ROLE__ || null
       };
     },
     computed: {
diff --git a/src/resources.js b/src/resources.js
index 6e19eae..d7a54e2 100644
--- a/src/resources.js
+++ b/src/resources.js
@@ -130,6 +130,27 @@ function getResourceURL(request_url) {
     return request_url; // webpacked
   }
 
+  // normalize already-resolved urls back to assets:// so this stays
+  // idempotent (the pack is served same-origin under /assetpack/)
+  if (assets_root) {
+    if (resource_url.startsWith(assets_root)) {
+      resource_url = "assets://" + resource_url.slice(assets_root.length);
+    } else {
+      try {
+        const root_path = new URL(
+          assets_root,
+          typeof window !== "undefined" ? window.location.origin : undefined
+        ).pathname;
+        if (root_path !== "/" && resource_url.startsWith(root_path)) {
+          resource_url = "assets://" + resource_url.slice(root_path.length);
+        }
+      } catch (e) {
+        // assets_root not url-shaped
+      }
+    }
+    request_url = resource_url;
+  }
+
   // Preliminary filtering
   resource_url = resource_url
     .replace(/^app:\/\/\.\//, "/") // app://./archive/ comes up on non-windows platforms, sometimes?
diff --git a/src/store/localData.js b/src/store/localData.js
index 21a3ef0..9e3200d 100644
--- a/src/store/localData.js
+++ b/src/store/localData.js
@@ -1,25 +1,26 @@
 import Vue from "vue";
-import axios from "axios";
 const semver = require("semver");
 
-// Set up axios to use JWT token from localStorage
-axios.interceptors.request.use(
-  config => {
-    const token = localStorage.getItem("jwt_token");
-    if (token) {
-      config.headers.Authorization = `Bearer ${token}`;
-    }
-    return config;
-  },
-  error => Promise.reject(error)
-);
+// saveData/settings/tabData persist to localStorage instead of the
+// upstream auth server
+const LOCALDATA_KEY = "tuhcLocalData";
 
-const API_BASE_URL =
-  typeof window !== "undefined" && window.webAppAuthServerUrl
-    ? `${window.webAppAuthServerUrl}/api`
-    : typeof process !== "undefined" && process.env.AUTH_SERVER_URL
-    ? `${process.env.AUTH_SERVER_URL}/api`
-    : "http://localhost:9413/api";
+function readPersisted() {
+  try {
+    return JSON.parse(localStorage.getItem(LOCALDATA_KEY)) || {};
+  } catch (e) {
+    console.error("Failed to parse persisted localData:", e);
+    return {};
+  }
+}
+
+function writePersisted(obj) {
+  try {
+    localStorage.setItem(LOCALDATA_KEY, JSON.stringify(obj));
+  } catch (e) {
+    console.error("Failed to persist localData:", e);
+  }
+}
 
 // isWebApp for main-process electron execution
 const isWebApp = (typeof window !== "undefined" && window.isWebApp) || false;
@@ -67,10 +68,8 @@ const migrations = {
 var log;
 if (!window.isWebApp) {
   const Store = require("electron-store");
-  // store = new Store({ migrations }); // No longer needed
   log = require("Logger");
 } else {
-  // store = require("@/../webapp/localstore.js"); // No longer needed
   log = {
     scope() {
       return console;
@@ -143,7 +142,7 @@ const DEFAULT_SETTINGS = {
   smoothScrolling: true,
   pixelScaling: true,
   mspaMode: false, // Ensure MSPA page numbers are disabled by default
-  bandcampEmbed: true,
+  bandcampEmbed: false, // offline build: no external embeds by default
   allowSysUpdateNotifs: true,
   lastCheckedUpdate: "",
 
@@ -183,6 +182,20 @@ const DEFAULT_SETTINGS = {
   cursedHistory: true,
   ruffleFallback: true,
 
+  // Touch controls overlay (see components/UIElements/TouchControls.vue)
+  touchControls: true,
+  touchAutoShow: true,
+  touchOpacity: 0.4,
+  touchScale: 1,
+  touchHaptics: true,
+  touchNavArrows: false,
+  touchSwipeNav: false,
+  touchLayout: null,
+  touchButtons: [
+    { label: "␣", key: " ", code: "Space", keyCode: 32 },
+    { label: "↵", key: "Enter", code: "Enter", keyCode: 13 }
+  ],
+
   // Dynamically enable mods based on what's available in the asset pack
   modListEnabled: getAvailableMods(),
   ...(window.webAppOpinionatedDefaults
@@ -216,7 +229,8 @@ class LocalData {
           loadedTabList: [],
           tabChainIndex: undefined,
           isPoppingState: false,
-          saveDebounce: false
+          saveDebounce: false,
+          touchEdit: false // signals TouchControls to enter layout-edit mode
         }
       }),
       computed: {
@@ -245,10 +259,7 @@ class LocalData {
       },
       methods: {
         _migrateStorage(new_version) {
-          // This method is a placeholder to prevent errors.
-          // Actual migration logic (if any) would go here.
-          // For now, we assume the data is already in the correct format.
-          // Skip migration logs in webapp mode to reduce console noise
+          // no migrations needed for the localStorage format yet
           if (!window.isWebApp) {
             console.log("Migration check for version", new_version, "skipped.");
           }
@@ -259,18 +270,29 @@ class LocalData {
             this.saveDebounce = undefined;
           }
           try {
-            await axios.put(`${API_BASE_URL}/data`, {
+            writePersisted({
               saveData: this.saveData,
-              settings: this.settings
+              settings: this.settings,
+              tabData: this.tabData
             });
-            if (window.isWebApp) {
-              this.$logger.debug("Data saved to server.");
-            } else {
-              this.$logger.info("Data saved to server.");
+            // mirror the flags mods.js reads through the localstore
+            if (window.localstore) {
+              for (const k of [
+                "jsFlashes",
+                "hqAudio",
+                "bolin",
+                "soluslunes",
+                "unpeachy",
+                "pxsTavros",
+                "newReader",
+                "ruffleFallback"
+              ]) {
+                window.localstore.set(`settings.${k}`, this.settings[k]);
+              }
             }
+            this.$logger.debug("Data saved to localStorage.");
           } catch (e) {
-            this.$logger.error("Failed to save data to server:", e);
-            // Optionally, fall back to local storage or show an error to the user
+            this.$logger.error("Failed to save data to localStorage:", e);
           }
         },
         applySaveIfPending() {
@@ -284,39 +306,25 @@ class LocalData {
         },
         async reloadLocalStorage() {
           this.applySaveIfPending();
-          try {
-            const response = await axios.get(`${API_BASE_URL}/session`);
-            const { saveData, settings } = response.data;
-            this.saveData = saveData;
-            this.settings = settings;
-            if (window.isWebApp) {
-              this.$logger.debug("Data reloaded from server.");
-            } else {
-              this.$logger.info("Data reloaded from server.");
-            }
-          } catch (e) {
-            this.$logger.error("Failed to reload data from server:", e);
-            // If no session or error, prompt for login/signup
-            // This part will be handled in the install method
-          }
-
-          // The rest of the reloadLocalStorage logic remains the same for tabData
-          // as tabData is not persisted on the server per user.
-          // If tabData needs to be persisted, it would require a separate API.
-          const all = {
-            assetDir: this.assetDir || "", // assetDir is not part of user data, so keep it local
-            tabData: this.tabData || DEFAULT_TABDATA // tabData is not part of user data, so keep it local
+          const stored = readPersisted();
+          this.saveData = {
+            ...DEFAULT_SAVEDATA,
+            ...(stored.saveData || this.saveData || {})
           };
+          this.settings = {
+            ...DEFAULT_SETTINGS,
+            ...(stored.settings || this.settings || {})
+          };
+          this.$logger.debug("Data reloaded from localStorage.");
+
           const back = {
-            assetDir: all["assetDir"] || "",
-            saveData: this.saveData || DEFAULT_SAVEDATA,
-            settings: { ...DEFAULT_SETTINGS, ...this.settings },
-            tabData: all["tabData"] || DEFAULT_TABDATA
+            assetDir: this.assetDir || "",
+            saveData: this.saveData,
+            settings: this.settings,
+            tabData: stored.tabData || this.tabData || DEFAULT_TABDATA
           };
 
-          this.assetDir = "web"; // Always set assetDir to "web"
-          // this.saveData = back.saveData; // Already set from API
-          // this.settings = back.settings; // Already set from API
+          this.assetDir = "web";
           if (this.settings.useTabbedBrowsing) {
             this.temp.isPoppingState = true;
             this.tabData = back.tabData;
@@ -330,13 +338,19 @@ class LocalData {
           }
         },
         clearLocalStorage() {
-          // This method will now clear local client-side state, not server data
-          this.applySaveIfPending();
+          if (this.saveDebounce) {
+            clearTimeout(this.saveDebounce);
+            this.saveDebounce = undefined;
+          }
+          try {
+            localStorage.removeItem(LOCALDATA_KEY);
+          } catch (e) {
+            console.error(e);
+          }
           this.assetDir = "web"; // Always set assetDir to "web"
           this.tabData = DEFAULT_TABDATA;
           this.saveData = DEFAULT_SAVEDATA;
           this.settings = DEFAULT_SETTINGS;
-          // No server call here, as this is a client-side reset
         },
         HISTORY_CLEAR() {
           this.tabData.tabList.forEach(k => {
@@ -838,58 +852,21 @@ export default {
     const the_store = new LocalData();
     the_store.VM._migrateStorage(window.appVersion);
 
-    // Check for existing session on app start
-    const checkSession = (retries = 3) => {
-      axios
-        .get(`${API_BASE_URL}/session`)
-        .then(response => {
-          the_store.VM.saveData = response.data.saveData;
-          the_store.VM.settings = response.data.settings;
-          the_store.VM.isAuthenticated = true; // Set authentication status
-          if (window.isWebApp) {
-            the_store.VM.$logger.debug(
-              "Session found. Data loaded from server."
-            );
-          } else {
-            the_store.VM.$logger.info(
-              "Session found. Data loaded from server."
-            );
-          }
-          the_store.VM.reloadLocalStorage(); // Reload local state (tabData, etc.)
-        })
-        .catch(error => {
-          // If unauthorized, clear token
-          if (error.response && error.response.status === 401) {
-            localStorage.removeItem("jwt_token");
-          }
-          the_store.VM.$logger.warn(
-            "No active session found or failed to load session:",
-            error
-          );
-          if (retries > 0) {
-            the_store.VM.$logger.info(
-              `Retrying session check (${retries} retries left)...`
-            );
-            setTimeout(() => checkSession(retries - 1), 1000); // Retry after 1 second
-            return;
-          }
-          // If no session, prompt user to login/signup
-          // This will be handled by a new component or a global event
-          // For now, we'll just load default data
-          the_store.VM.saveData = DEFAULT_SAVEDATA;
-          the_store.VM.settings = DEFAULT_SETTINGS;
-          the_store.VM.isAuthenticated = false; // Set authentication status
-          the_store.VM.reloadLocalStorage(); // Reload local state (tabData, etc.)
-        })
-        .finally(() => {
-          // Set sessionChecked to true in the root Vue instance
-          if (window.vm && window.vm.$refs.App) {
-            window.vm.$refs.App.sessionChecked = true;
-          }
-        });
+    // no auth server: load persisted state and mark the session ready
+    const loadPersistedState = () => {
+      the_store.VM.isAuthenticated = true;
+      the_store.VM.reloadLocalStorage();
+      const signalReady = (tries = 20) => {
+        if (window.vm && window.vm.$refs.App) {
+          window.vm.$refs.App.sessionChecked = true;
+        } else if (tries > 0) {
+          setTimeout(() => signalReady(tries - 1), 50);
+        }
+      };
+      signalReady();
     };
 
-    checkSession();
+    loadPersistedState();
 
     Vue.mixin({
       beforeCreate() {
diff --git a/vue.config.js b/vue.config.js
index 01495f9..11cb452 100644
--- a/vue.config.js
+++ b/vue.config.js
@@ -1,7 +1,9 @@
 const webpack = require("webpack");
 const { execSync } = require("child_process");
+const path = require("path");
 
 const is_webapp = Boolean(process.env.ASSET_PACK_HREF);
+const local = p => path.resolve(__dirname, p);
 
 module.exports = {
   configureWebpack: {
@@ -27,13 +29,15 @@ module.exports = {
         vue$: "vue/dist/vue.esm.js",
         "@/*": "./src/*",
         // "IpcRenderer$": '/src/js/ipcRendererAlias.js'
-        Logger$: is_webapp ? "/webapp/logPolyfill.js" : "electron-log",
+        Logger$: is_webapp ? local("webapp/logPolyfill.js") : "electron-log",
         IpcRenderer$: is_webapp
-          ? "/webapp/fakeIpc.js"
-          : "/src/js/ipcRendererAlias.js",
+          ? local("webapp/fakeIpc.js")
+          : local("src/js/ipcRendererAlias.js"),
         "electron-store$": is_webapp
-          ? "/webapp/storePolyfill.js"
-          : "electron-store"
+          ? local("webapp/storePolyfill.js")
+          : "electron-store",
+        // Stub the bare electron module; all uses are guarded by !isWebApp
+        ...(is_webapp ? { electron$: local("webapp/electronStub.js") } : {})
       }
     },
     plugins: [],
diff --git a/webapp/fakeIpc.js b/webapp/fakeIpc.js
index baef597..5c524a4 100644
--- a/webapp/fakeIpc.js
+++ b/webapp/fakeIpc.js
@@ -117,17 +117,10 @@ const fakeIpc = {
 // Object.entries(invokables).forEach(i => fakeIpc.handle(i[0], (event, args) => i[1](args)))
 Object.entries(invokables).forEach(i => fakeIpc.handle(i[0], i[1]));
 
-// import axios from 'axios'
+// no external request proxying in the offline build
 fakeIpc.handle("request", async (_, axios_request) => {
-  const result = await fetch(
-    "https://corsproxy.io/?" + encodeURIComponent(axios_request.url),
-    {
-      ...axios_request,
-      body: JSON.stringify(axios_request.data)
-    }
-  );
-  // const result = await axios(axios_request)
-  return { data: await result.text(), status: result.status };
+  console.warn("fakeIpc: 'request' blocked in offline build:", axios_request);
+  return { data: "", status: 0 };
 });
 
 module.exports = fakeIpc;
